# An ArchUnit rule whose `should` clause matches no classes passes by default,
# which means a renamed or emptied package silently stops being guarded. Both
# services are now fully implemented, so every rule below has real classes to
# match and an empty match is a genuine regression rather than a skeleton.
archRule.failOnEmptyShould=true
